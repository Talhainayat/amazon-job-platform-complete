import { FormEvent, useState } from 'react'
import { apiError, contactApi } from '../services/api'

export default function ContactPage() {
  const [sent, setSent] = useState(false); const [error, setError] = useState(''); const [busy, setBusy] = useState(false)
  const submit = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); setBusy(true); setError(''); const form = new FormData(event.currentTarget); try { await contactApi.create({ name: String(form.get('name')), email: String(form.get('email')), phone: String(form.get('phone') || ''), message: String(form.get('message')) }); setSent(true); event.currentTarget.reset() } catch (err) { setError(apiError(err, 'We could not send your message. Please try again.')) } finally { setBusy(false) } }
  return <main className="inner-public contact-layout"><section className="page-hero"><p className="eyebrow">START A CONVERSATION</p><h1>Let’s make the next move clearer.</h1><p>Tell us what you are looking for and the placement desk will get back to you.</p><div className="contact-details"><strong>+92 333 2158308</strong><span>hello@talentpath.pk</span><span>Lahore, Pakistan · Remote support worldwide</span></div></section><form className="contact-form" onSubmit={submit}><div className="form-heading"><span>01 / INQUIRY</span><h2>How can we help?</h2></div>{sent && <div className="alert success">Thanks. Your inquiry is with our placement desk.</div>}{error && <div className="alert error">{error}</div>}<label className="field"><span>Name</span><input name="name" required /></label><label className="field"><span>Email</span><input name="email" type="email" required /></label><label className="field"><span>Phone</span><input name="phone" /></label><label className="field"><span>Message</span><textarea name="message" minLength={10} required /></label><button className="btn" disabled={busy}>{busy ? 'Sending…' : 'Send inquiry →'}</button></form><section className="faq"><p className="eyebrow">QUICK ANSWERS</p><details open><summary>Do I need an existing Amazon account?</summary><p>We can help you understand the next step, but you will use your own account to claim a slot.</p></details><details><summary>Can a manager use TalentPath?</summary><p>Yes. Contact us to discuss manager portal access and candidate workflow support.</p></details></section></main>
}